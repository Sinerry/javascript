课堂练习：
1. 单击按钮，向div中添加一个p标签，
p标签内容是一个随机字母（大小写均可），
并实现p标签的隔行变色




















2. 一个密码框，失去焦点对其校验，显示提示信息，规则如下：
a. 密码不能为空
b. 密码长度为6-16之间


c. 并给予密码的强中弱提示
	强：字母，数字，特殊字符都有
	中：有字母，数字，特殊字符其中的两种
	弱：要么全是数字，要么全是字母，要么全是特殊字符














